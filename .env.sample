# OpenCode 部署环境变量模板
# 复制为 .env 后填写实际值，chmod 600 .env
# 切勿提交 .env 到 git

# 时区
TZ=Asia/Shanghai

# OpenCode Web UI 登录凭证（请使用强密码：openssl rand -base64 24）
OPENCODE_SERVER_USERNAME=your_username_here
OPENCODE_SERVER_PASSWORD=change_me_to_a_strong_password
